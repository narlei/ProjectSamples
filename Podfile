# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ProjectSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'ObjectMapper'
  pod 'Realm', '3.0.0-beta.2'
  pod 'Moya', '9.0.0-alpha.1'
  # Pods for ProjectSample

  target 'ProjectSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ProjectSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
